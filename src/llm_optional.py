import os

def tailor_resume(master_latex: str, job_description: str) -> str:
    # Fully free mode: do not call AI; return template unchanged.
    # Later, you can implement Gemini/OpenAI calls here.
    return master_latex
